on:
  workflow_call:
    inputs:
      matrix:
        type: string

jobs:
  dynamic-matrix:
    strategy:
      matrix:  ${{ fromJSON(inputs.matrix) }}
    runs-on: ubuntu-latest
    # env:
    #   value: ${{ fromJSON(inputs.matrix) }}
    steps: 
      - run: echo $value
on:
  workflow_call:
    inputs:
      matrix:
        type: string

jobs:
  dynamic-matrix:
    strategy:
      matrix:  ${{ fromJSON(inputs.matrix).values }}
    runs-on: ubuntu-latest
    env:
      value: ${{ matrix.project }}
    steps: 
      - run: echo $value